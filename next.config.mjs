/** @type {import('next').NextConfig} */
const nextConfig = {
  // Comment out output: 'export' for local development
  //output: 'export',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
